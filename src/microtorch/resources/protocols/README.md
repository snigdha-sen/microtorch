Protocols

This directory contains MRI acquisition protocol files bundled with microTorch. These protocols are installed with the package and can be accessed programmatically using importlib.resources.

They are used by the simulation and fitting utilities and can also be used when running microTorch on custom data.